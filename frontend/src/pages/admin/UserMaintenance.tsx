
import React from 'react';

const UserMaintenance: React.FC = () => {
    const message = "Hello, welcome to our website!";

    return (
        <div style={{ padding: "20px", fontSize: "20px", color: "blue", textAlign: "center" }}>
            {message}
        </div>
    );
};

export default UserMaintenance;
